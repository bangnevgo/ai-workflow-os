# Divisi Penjualan — folder kerja `penjualan`

Agen/platform AI yang dipanggil dengan working directory di sini dianggap bekerja di divisi ini — ditentukan oleh sistem, bukan klaim model.

Satu-satunya cara mencatat status:

    workflow submit --project <proyek> --status <STATUS> [--evidence ...]

Lihat `workflow states` dan `workflow whoami`.
